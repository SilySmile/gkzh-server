<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gkzh.wjdc.mapper.WjdcSurveyMapper">
    
    <resultMap type="WjdcSurvey" id="WjdcSurveyResult">
        <result property="id"    column="id"    />
        <result property="title"    column="title"    />
        <result property="description"    column="description"    />
        <result property="status"    column="status"    />
        <result property="startTime"    column="start_time"    />
        <result property="endTime"    column="end_time"    />
        <result property="createdAt"    column="created_at"    />
    </resultMap>

    <sql id="selectWjdcSurveyVo">
        select id, title, description, status, start_time, end_time, created_at from wjdc_survey
    </sql>

    <select id="selectWjdcSurveyList" parameterType="WjdcSurvey" resultMap="WjdcSurveyResult">
        <include refid="selectWjdcSurveyVo"/>
        <where>  
            <if test="title != null  and title != ''"> and title like concat('%', #{title}, '%')</if>
            <if test="status != null "> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectWjdcSurveyById" parameterType="Long" resultMap="WjdcSurveyResult">
        <include refid="selectWjdcSurveyVo"/>
        where id = #{id}
    </select>

    <insert id="insertWjdcSurvey" parameterType="WjdcSurvey" useGeneratedKeys="true" keyProperty="id">
        insert into wjdc_survey
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null and title != ''">title,</if>
            <if test="description != null">description,</if>
            <if test="status != null">status,</if>
            <if test="startTime != null">start_time,</if>
            <if test="endTime != null">end_time,</if>
            <if test="createdAt != null">created_at,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null and title != ''">#{title},</if>
            <if test="description != null">#{description},</if>
            <if test="status != null">#{status},</if>
            <if test="startTime != null">#{startTime},</if>
            <if test="endTime != null">#{endTime},</if>
            <if test="createdAt != null">#{createdAt},</if>
         </trim>
    </insert>

    <update id="updateWjdcSurvey" parameterType="WjdcSurvey">
        update wjdc_survey
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="createdAt != null">created_at = #{createdAt},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteWjdcSurveyById" parameterType="Long">
        delete from wjdc_survey where id = #{id}
    </delete>

    <delete id="deleteWjdcSurveyByIds" parameterType="String">
        delete from wjdc_survey where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
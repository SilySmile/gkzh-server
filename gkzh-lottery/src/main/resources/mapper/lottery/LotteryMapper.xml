<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gkzh.lottery.mapper.LotteryMapper">
    
    <resultMap type="com.gkzh.lottery.domain.Lottery" id="LotteryResult">
        <result property="lotteryId"    column="lottery_id"    />
        <result property="title"    column="title"    />
        <result property="description"    column="description"    />
        <result property="startTime"    column="start_time"    />
        <result property="endTime"    column="end_time"    />
        <result property="status"    column="status"    />
    </resultMap>

    <sql id="selectLotteryVo">
        select lottery_id, title, description, start_time, end_time, status from lottery
    </sql>

    <select id="selectLotteryList" parameterType="com.gkzh.lottery.domain.Lottery" resultMap="LotteryResult">
        <include refid="selectLotteryVo"/>
        <where>  
            <if test="title != null  and title != ''"> and title = #{title}</if>
            <if test="description != null  and description != ''"> and description = #{description}</if>
            <if test="startTime != null "> and start_time = #{startTime}</if>
            <if test="endTime != null "> and end_time = #{endTime}</if>
            <if test="status != null "> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectLotteryByLotteryId" parameterType="Long" resultMap="LotteryResult">
        <include refid="selectLotteryVo"/>
        where lottery_id = #{lotteryId}
    </select>

    <insert id="insertLottery" parameterType="com.gkzh.lottery.domain.Lottery">
        insert into lottery
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="lotteryId != null">lottery_id,</if>
            <if test="title != null">title,</if>
            <if test="description != null">description,</if>
            <if test="startTime != null">start_time,</if>
            <if test="endTime != null">end_time,</if>
            <if test="status != null">status,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="lotteryId != null">#{lotteryId},</if>
            <if test="title != null">#{title},</if>
            <if test="description != null">#{description},</if>
            <if test="startTime != null">#{startTime},</if>
            <if test="endTime != null">#{endTime},</if>
            <if test="status != null">#{status},</if>
         </trim>
    </insert>

    <update id="updateLottery" parameterType="com.gkzh.lottery.domain.Lottery">
        update lottery
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="status != null">status = #{status},</if>
        </trim>
        where lottery_id = #{lotteryId}
    </update>

    <delete id="deleteLotteryByLotteryId" parameterType="Long">
        delete from lottery where lottery_id = #{lotteryId}
    </delete>

    <delete id="deleteLotteryByLotteryIds" parameterType="String">
        delete from lottery where lottery_id in 
        <foreach item="lotteryId" collection="array" open="(" separator="," close=")">
            #{lotteryId}
        </foreach>
    </delete>
</mapper>
